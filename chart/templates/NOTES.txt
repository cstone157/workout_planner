Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }} and installed in namespace {{ .Values.namespace.name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get values {{ .Release.Name }}
  $ helm get manifest {{ .Release.Name }}

{{- if .Values.frontend.service.type | eq "LoadBalancer" }}

Frontend Service:
  NAME: {{ include "workout-planner.fullname" . }}-frontend
  TYPE: {{ .Values.frontend.service.type }}
  
  You can access your frontend using:
  $ kubectl get svc {{ include "workout-planner.fullname" . }}-frontend -n {{ .Values.namespace.name }}

{{- else if .Values.frontend.service.type | eq "NodePort" }}

Frontend Service:
  NAME: {{ include "workout-planner.fullname" . }}-frontend
  TYPE: {{ .Values.frontend.service.type }}
  PORT: {{ .Values.frontend.service.port }}
  TARGET_PORT: {{ .Values.frontend.service.targetPort }}

  You can access your frontend using:
  $ kubectl port-forward -n {{ .Values.namespace.name }} svc/{{ include "workout-planner.fullname" . }}-frontend 3000:{{ .Values.frontend.service.targetPort }}

{{- else }}

Frontend Service:
  NAME: {{ include "workout-planner.fullname" . }}-frontend
  TYPE: {{ .Values.frontend.service.type }}
  PORT: {{ .Values.frontend.service.port }}
  TARGET_PORT: {{ .Values.frontend.service.targetPort }}

{{- end }}

Backend Service:
  NAME: {{ include "workout-planner.fullname" . }}-backend
  TYPE: {{ .Values.backend.service.type }}
  PORT: {{ .Values.backend.service.port }}
  TARGET_PORT: {{ .Values.backend.service.targetPort }}

MongoDB Service:
  NAME: {{ include "workout-planner.fullname" . }}-mongodb
  REPLICAS: {{ .Values.mongodb.replicas }}

Check deployment status:
  $ kubectl get deployments -n {{ .Values.namespace.name }}
  $ kubectl get pods -n {{ .Values.namespace.name }}
  $ kubectl get svc -n {{ .Values.namespace.name }}

View logs:
  $ kubectl logs -n {{ .Values.namespace.name }} -l app.kubernetes.io/name=workout-planner

For more information visit: https://github.com/yourusername/workout-planner

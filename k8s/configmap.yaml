apiVersion: v1
kind: ConfigMap
metadata:
  name: workout-planner-config
  namespace: workout-planner
data:
  BACKEND_PORT: "8080"
  MCP_PORT: "8081"
  CHROMA_PORT: "8000"
  OLLAMA_PORT: "11434"
  FRONTEND_URL: "http://svelte-frontend.workout-planner.svc.cluster.local"
